import GObject from "gi://GObject";
import St from "gi://St";

export const Graph = GObject.registerClass(
  class Graph extends St.DrawingArea {
    constructor(width, height, parent) {
      this.set
    }

    _draw() {
      let cr = this.ac
    }
  }
);
